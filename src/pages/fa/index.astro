---
import BaseLayout from '../../layouts/BaseLayout.astro';
import CountryCard from '../../components/CountryCard.astro';
import Button from '../../components/ui/Button.astro';
import Card from '../../components/ui/Card.astro';
import { getTranslations, type SupportedLang } from '../../lib/i18n';

const lang: SupportedLang = 'fa';
const i18n = getTranslations(lang);

// Country data with recipient counts
const countries = [
  { code: 'us', flag: '🇺🇸', recipientCount: 12 },
  { code: 'uk', flag: '🇬🇧', recipientCount: 8 },
  { code: 'de', flag: '🇩🇪', recipientCount: 6 },
  { code: 'fr', flag: '🇫🇷', recipientCount: 5 },
  { code: 'ca', flag: '🇨🇦', recipientCount: 4 },
];
---

<BaseLayout title={i18n.home.title} lang={lang}>
  <div class="max-w-4xl mx-auto">
    <!-- Hero Section -->
    <section class="text-center py-lg">
      <h1 class="text-4xl md:text-5xl font-bold text-text mb-md">
        {i18n.home.title}
      </h1>
      <p class="text-xl text-text/80 mb-lg max-w-2xl mx-auto">
        {i18n.home.heroDescription}
      </p>
    </section>

    <!-- Crisis Alert -->
    <Card variant="bordered" padding="lg" class="mb-lg border-urgent/50 bg-urgent/5">
      <div class="flex items-start gap-md">
        <span class="text-urgent text-2xl">⚠️</span>
        <div>
          <h2 class="text-lg font-semibold text-urgent mb-xs">{i18n.home.currentCrisis}</h2>
          <p class="text-text/80">{i18n.home.crisisDescription}</p>
        </div>
      </div>
    </Card>

    <!-- Country Selection -->
    <section class="py-md">
      <h2 class="text-2xl font-semibold text-text mb-md text-center">
        {i18n.home.selectCountry}
      </h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-md">
        {countries.map((country) => (
          <CountryCard 
            country={country.code}
            flag={country.flag}
            recipientCount={country.recipientCount}
            lang={lang}
          />
        ))}
      </div>
    </section>

    <!-- CTA Section -->
    <section class="text-center py-lg">
      <Button variant="primary" href="#countries">
        {i18n.home.getStarted}
      </Button>
    </section>
  </div>
</BaseLayout>

